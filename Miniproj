package miniproject;

import java.util.ArrayList;
import java.util.Scanner;
//===== BANK MANAGEMENT SYSTEM =====

//1. Create Account
//2. Deposits Money
//3. Withdraw Money
//4. Check Balance
//5. Transfer Money
//6. View Account Details
//7. Transaction History
//8. Exit
// ================= ACCOUNT CLASS =================

class Account {

    private int accountNumber;
    private String holderName;
    private String accountType;
    private double balance;

    private ArrayList<String> transactions;

    public Account(int accountNumber, String holderName,
                   String accountType, double initialBalance) {

        this.accountNumber = accountNumber;
        this.holderName = holderName;
        this.accountType = accountType;
        this.balance = initialBalance;

        transactions = new ArrayList<>();

        transactions.add("Account opened with Rs." + initialBalance);
    }

    public int getAccountNumber() {
        return accountNumber;
    }

    public String getHolderName() {
        return holderName;
    }

    public String getAccountType() {
        return accountType;
    }

    public double getBalance() {
        return balance;
    }

    // ================= DEPOSIT =================

    public void deposit(double amount) {

        if (amount <= 0) {
            System.out.println("Invalid deposit amount!");
            return;
        }

        balance += amount;

        transactions.add("Deposited Rs." + amount);

        System.out.println("Deposit successful!");
        System.out.println("Current Balance: Rs." + balance);
    }

    // ================= WITHDRAW =================

    public void withdraw(double amount) {

        if (amount <= 0) {
            System.out.println("Invalid withdrawal amount!");
            return;
        }

        if (amount > balance) {
            System.out.println("Insufficient balance!");
            return;
        }

        balance -= amount;

        transactions.add("Withdrawn Rs." + amount);

        System.out.println("Withdrawal successful!");
        System.out.println("Current Balance: Rs." + balance);
    }

    // ================= TRANSACTION HISTORY =================

    public void showTransactions() {

        System.out.println("\n===== TRANSACTION HISTORY =====");

        if (transactions.isEmpty()) {
            System.out.println("No transactions found.");
            return;
        }

        for (String transaction : transactions) {
            System.out.println("- " + transaction);
        }
    }

    // ================= ACCOUNT DETAILS =================

    public void displayAccountDetails() {

        System.out.println("\n===== ACCOUNT DETAILS =====");

        System.out.println("Account Number : " + accountNumber);
        System.out.println("Holder Name    : " + holderName);
        System.out.println("Account Type   : " + accountType);
        System.out.println("Balance        : Rs." + balance);
    }
}


// ================= BANK CLASS =================

class Bank {

    private ArrayList<Account> accounts;

    private int nextAccountNumber = 1001;

    public Bank() {
        accounts = new ArrayList<>();
    }

    // ================= CREATE ACCOUNT =================

    public void createAccount(String name,
                               String type,
                               double initialDeposit) {

        if (initialDeposit < 500) {

            System.out.println("Minimum initial deposit is Rs.500.");
            return;
        }

        Account account = new Account(
                nextAccountNumber,
                name,
                type,
                initialDeposit
        );

        accounts.add(account);

        System.out.println("\n===== ACCOUNT CREATED SUCCESSFULLY =====");

        System.out.println("Account Number : " + nextAccountNumber);
        System.out.println("Holder Name    : " + name);
        System.out.println("Account Type   : " + type);
        System.out.println("Initial Deposit: Rs." + initialDeposit);

        nextAccountNumber++;
    }

    // ================= FIND ACCOUNT =================

    public Account findAccount(int accountNumber) {

        for (Account account : accounts) {

            if (account.getAccountNumber() == accountNumber) {
                return account;
            }
        }

        return null;
    }

    // ================= DEPOSIT =================

    public void deposit(int accountNumber, double amount) {

        Account account = findAccount(accountNumber);

        if (account == null) {

            System.out.println("Account not found!");
            return;
        }

        account.deposit(amount);
    }

    // ================= WITHDRAW =================

    public void withdraw(int accountNumber, double amount) {

        Account account = findAccount(accountNumber);

        if (account == null) {

            System.out.println("Account not found!");
            return;
        }

        account.withdraw(amount);
    }

    // ================= CHECK BALANCE =================

    public void checkBalance(int accountNumber) {

        Account account = findAccount(accountNumber);

        if (account == null) {

            System.out.println("Account not found!");
            return;
        }

        System.out.println("\n===== ACCOUNT BALANCE =====");

        System.out.println("Account Number : "
                + account.getAccountNumber());

        System.out.println("Holder Name    : "
                + account.getHolderName());

        System.out.println("Balance        : Rs."
                + account.getBalance());
    }

    // ================= ACCOUNT DETAILS =================

    public void showAccountDetails(int accountNumber) {

        Account account = findAccount(accountNumber);

        if (account == null) {

            System.out.println("Account not found!");
            return;
        }

        account.displayAccountDetails();
    }

    // ================= TRANSFER MONEY =================

    public void transferMoney(int fromAccount,
                              int toAccount,
                              double amount) {

        Account sender = findAccount(fromAccount);

        Account receiver = findAccount(toAccount);

        if (sender == null) {

            System.out.println("Sender account not found!");
            return;
        }

        if (receiver == null) {

            System.out.println("Receiver account not found!");
            return;
        }

        if (fromAccount == toAccount) {

            System.out.println(
                    "Cannot transfer money to the same account!"
            );

            return;
        }

        if (amount <= 0) {

            System.out.println("Invalid transfer amount!");
            return;
        }

        if (amount > sender.getBalance()) {

            System.out.println("Insufficient balance!");
            return;
        }

        // Withdraw from sender
        sender.withdraw(amount);

        // Deposit to receiver
        receiver.deposit(amount);

        System.out.println("\n===== TRANSFER SUCCESSFUL =====");

        System.out.println("From Account : " + fromAccount);
        System.out.println("To Account   : " + toAccount);
        System.out.println("Amount       : Rs." + amount);
    }

    // ================= SHOW ALL ACCOUNTS =================

    public void showAllAccounts() {

        System.out.println("\n========== ALL ACCOUNTS ==========");

        if (accounts.isEmpty()) {

            System.out.println("No accounts available.");
            return;
        }

        for (Account account : accounts) {

            System.out.println("--------------------------------");

            System.out.println("Account Number : "
                    + account.getAccountNumber());

            System.out.println("Holder Name    : "
                    + account.getHolderName());

            System.out.println("Account Type   : "
                    + account.getAccountType());

            System.out.println("Balance        : Rs."
                    + account.getBalance());
        }

        System.out.println("--------------------------------");
    }

    // ================= TRANSACTION HISTORY =================

    public void showTransactionHistory(int accountNumber) {

        Account account = findAccount(accountNumber);

        if (account == null) {

            System.out.println("Account not found!");
            return;
        }

        account.showTransactions();
    }
}


// ================= MAIN CLASS =================

public class Miniproject {

    public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);

        Bank bank = new Bank();

        int choice = 0;

        System.out.println("======================================");
        System.out.println("       WELCOME TO JAVA BANK");
        System.out.println("======================================");

        do {

            System.out.println("\n========== BANK MANAGEMENT SYSTEM ==========");

            System.out.println("1. Create Account");
            System.out.println("2. Deposit Money");
            System.out.println("3. Withdraw Money");
            System.out.println("4. Check Balance");
            System.out.println("5. Transfer Money");
            System.out.println("6. View Account Details");
            System.out.println("7. Transaction History");
            System.out.println("8. View All Accounts");
            System.out.println("9. Exit");

            System.out.println("============================================");

            System.out.print("Enter your choice: ");

            try {

                choice = scanner.nextInt();

                switch (choice) {

                    // ================= CREATE ACCOUNT =================

                    case 1:

                        scanner.nextLine();

                        System.out.print(
                                "Enter Account Holder Name: "
                        );

                        String name = scanner.nextLine();

                        System.out.print(
                                "Enter Account Type (Savings/Current): "
                        );

                        String type = scanner.nextLine();

                        System.out.print(
                                "Enter Initial Deposit: Rs."
                        );

                        double initialDeposit = scanner.nextDouble();

                        bank.createAccount(
                                name,
                                type,
                                initialDeposit
                        );

                        break;


                    // ================= DEPOSIT =================

                    case 2:

                        System.out.print(
                                "Enter Account Number: "
                        );

                        int depositAccount = scanner.nextInt();

                        System.out.print(
                                "Enter Deposit Amount: Rs."
                        );

                        double depositAmount = scanner.nextDouble();

                        bank.deposit(
                                depositAccount,
                                depositAmount
                        );

                        break;


                    // ================= WITHDRAW =================

                    case 3:

                        System.out.print(
                                "Enter Account Number: "
                        );

                        int withdrawAccount = scanner.nextInt();

                        System.out.print(
                                "Enter Withdrawal Amount: Rs."
                        );

                        double withdrawAmount = scanner.nextDouble();

                        // FIXED
                        bank.withdraw(
                                withdrawAccount,
                                withdrawAmount
                        );

                        break;


                    // ================= BALANCE =================

                    case 4:

                        System.out.print(
                                "Enter Account Number: "
                        );

                        int balanceAccount = scanner.nextInt();

                        bank.checkBalance(balanceAccount);

                        break;


                    // ================= TRANSFER =================

                    case 5:

                        System.out.print(
                                "Enter Sender Account Number: "
                        );

                        int sender = scanner.nextInt();

                        System.out.print(
                                "Enter Receiver Account Number: "
                        );

                        int receiver = scanner.nextInt();

                        System.out.print(
                                "Enter Transfer Amount: Rs."
                        );

                        double transferAmount = scanner.nextDouble();

                        bank.transferMoney(
                                sender,
                                receiver,
                                transferAmount
                        );

                        break;


                    // ================= ACCOUNT DETAILS =================

                    case 6:

                        System.out.print(
                                "Enter Account Number: "
                        );

                        int detailsAccount = scanner.nextInt();

                        bank.showAccountDetails(
                                detailsAccount
                        );

                        break;


                    // ================= TRANSACTION HISTORY =================

                    case 7:

                        System.out.print(
                                "Enter Account Number: "
                        );

                        int transactionAccount = scanner.nextInt();

                        bank.showTransactionHistory(
                                transactionAccount
                        );

                        break;


                    // ================= ALL ACCOUNTS =================

                    case 8:

                        bank.showAllAccounts();

                        break;


                    // ================= EXIT =================

                    case 9:

                        System.out.println(
                                "\nThank you for using Java Bank!"
                        );

                        System.out.println(
                                "Have a nice day!"
                        );

                        break;


                    default:

                        System.out.println(
                                "Invalid choice! Please try again."
                        );
                }

            } catch (Exception e) {

                System.out.println(
                        "Invalid input! Please enter valid data."
                );

                scanner.nextLine();

                choice = 0;
            }

        } while (choice != 9);

        scanner.close();
    }

(OUTPUT)=>======================================
       WELCOME TO JAVA BANK
======================================

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 1
Enter Account Holder Name: ranjith
Enter Account Type (Savings/Current): saving
Enter Initial Deposit: Rs.2000

===== ACCOUNT CREATED SUCCESSFULLY =====
Account Number : 1001
Holder Name    : ranjith
Account Type   : saving
Initial Deposit: Rs.2000.0

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 2
Enter Account Number: 1001
Enter Deposit Amount: Rs.2000
Deposit successful!
Current Balance: Rs.4000.0

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 3
Enter Account Number: 1001
Enter Withdrawal Amount: Rs.2000
Withdrawal successful!
Current Balance: Rs.2000.0

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 4
Enter Account Number: 1001

===== ACCOUNT BALANCE =====
Account Number : 1001
Holder Name    : ranjith
Balance        : Rs.2000.0

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 5
Enter Sender Account Number: 1002
Enter Receiver Account Number: 

1003
Enter Transfer Amount: Rs.2000
Sender account not found!

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 5
Enter Sender Account Number: 1001
Enter Receiver Account Number: 1002
Enter Transfer Amount: Rs.500
Receiver account not found!

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 1
Enter Account Holder Name: ramu
Enter Account Type (Savings/Current): saving
Enter Initial Deposit: Rs.4000

===== ACCOUNT CREATED SUCCESSFULLY =====
Account Number : 1002
Holder Name    : ramu
Account Type   : saving
Initial Deposit: Rs.4000.0

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 5
Enter Sender Account Number: 1001
Enter Receiver Account Number: 1002
Enter Transfer Amount: Rs.500
Withdrawal successful!
Current Balance: Rs.1500.0
Deposit successful!
Current Balance: Rs.4500.0

===== TRANSFER SUCCESSFUL =====
From Account : 1001
To Account   : 1002
Amount       : Rs.500.0

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 6
Enter Account Number: 1001

===== ACCOUNT DETAILS =====
Account Number : 1001
Holder Name    : ranjith
Account Type   : saving
Balance        : Rs.1500.0

========== BANK MANAGEMENT SYSTEM ==========
1. Create Account
2. Deposit Money
3. Withdraw Money
4. Check Balance
5. Transfer Money
6. View Account Details
7. Transaction History
8. View All Accounts
9. Exit
============================================
Enter your choice: 9

Thank you for using Java Bank!
Have a nice day!.

}

....................DashBoard..................
package textiles;

import java.awt.Color;
import java.awt.Font;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;

public class Dashboard implements ActionListener {

    JFrame frame;

    JButton logoutButton;

    String userName;

    public Dashboard(String userName) {

        this.userName = userName;

        frame = new JFrame(
                "Global Banking Management System - Dashboard"
        );

        frame.setSize(1100, 700);

        frame.setLayout(null);

        frame.setLocationRelativeTo(null);

        // HEADER

        JPanel header = new JPanel();

        header.setBounds(
                0, 0, 1100, 100
        );

        header.setLayout(null);

        header.setBackground(
                new Color(100, 30, 40)
        );

        frame.add(header);

        // LOGO / TITLE

        JLabel title =
                new JLabel("🏦 Global Banking Management System ");

        title.setBounds(
                40, 20, 400, 40
        );

        title.setFont(
                new Font("Serif", Font.BOLD, 30)
        );

        title.setForeground(Color.yellow);

        header.add(title);

        // USER

        JLabel user =
                new JLabel("Welcome, " + userName);

        user.setBounds(
                750, 25, 250, 30
        );

        user.setFont(
                new Font("Arial", Font.BOLD, 16)
        );

        user.setForeground(Color.gray);

        header.add(user);

        // MAIN TITLE

        JLabel dashboardTitle =
                new JLabel("GLOBAL BANKING MANAGEMENT DASHBOARD");

        dashboardTitle.setBounds(
                350, 130, 500, 40
        );

        dashboardTitle.setFont(
                new Font("Arial", Font.BOLD, 24)
        );

        dashboardTitle.setForeground(
                new Color(100, 30, 40)
        );

        frame.add(dashboardTitle);
     // CARD 1

        createCard(
                "👤",
                "profile",
                "10000",
                10000
                
        		);
     // CARD 2

        createCard(
                "💰",
                "Check Balance",
                "100000",
                100000
                
                
                
        );
        
                
      // CARD 3

        createCard(
                "💵",
                "Cash Deposit",
                "100000",
                100000
                
                
                
        );

        // CARD 4

        createCard(
                "➖",
                "with Draw",
                "20,000",
                20000
                
        );

        // CARD 5

        createCard(
                "💰",
                "Cheque",
                "50,000",
                50000
                
                
        );
     // CARD 6

        createCard(
                "📜",
                "Transfer Money",
                "50,0000",
                500000
                
                
        );
        
        // CARD 7

        createCard(
                "🚪",
                "Log Out",
                "50,0000",
                500000
                
                
        );
        
        JLabel collection =
                new JLabel("Global Banking Management System");

        collection.setBounds(
                400, 410, 350, 35
        );

        collection.setFont(
                new Font("Serif", Font.BOLD, 22)
        );

        collection.setForeground(
                new Color(100, 30, 40)
        );

        frame.add(collection);

        JLabel categories =
                new JLabel(
                     "Deposit   |   Withdrawal  |   Transfer   |   Transaction ID| Date & Time | Amount | Balance after transaction "
                		);

        categories.setBounds(
                250, 460, 650, 35
        );

        categories.setFont(
                new Font("Arial", Font.BOLD, 16)
        );

        categories.setForeground(
                new Color(100, 80, 60)
        );

        frame.add(categories);

        // LOGOUT

        logoutButton =
                new JButton("LOGOUT");

        logoutButton.setBounds(
                450, 550, 180, 45
        );

        logoutButton.setFont(
                new Font("Arial", Font.BOLD, 15)
        );

        logoutButton.setBackground(
                new Color(100, 30, 40)
        );

        logoutButton.setForeground(Color.WHITE);

        logoutButton.setFocusPainted(false);

        logoutButton.addActionListener(this);

        frame.add(logoutButton);

        frame.setDefaultCloseOperation(
                JFrame.EXIT_ON_CLOSE
        );

        frame.setVisible(true);
    }

    private void createCard(
            String icon,
            String title,
            String value,
            int x
    ) {

        JPanel card = new JPanel();

        card.setLayout(null);

        card.setBounds(
                x, 200, 250, 160
        );

        card.setBackground(
                new Color(255, 248, 235)
        );

        frame.add(card);

        JLabel iconLabel =
                new JLabel(icon);

        iconLabel.setBounds(
                95, 10, 60, 40
        );

        iconLabel.setFont(
                new Font("Arial", Font.PLAIN, 30)
        );

        card.add(iconLabel);

        JLabel titleLabel =
                new JLabel(title);

        titleLabel.setBounds(
                75, 55, 120, 25
        );

        titleLabel.setFont(
                new Font("Arial", Font.BOLD, 15)
        );

        titleLabel.setForeground(
                new Color(100, 30, 40)
        );

        card.add(titleLabel);

        JLabel valueLabel =
                new JLabel(value);

        valueLabel.setBounds(
                70, 90, 150, 40
        );

        valueLabel.setFont(
                new Font("Arial", Font.BOLD, 25)
        );

        card.add(valueLabel);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

        if (e.getSource() == logoutButton) {

            frame.dispose();

            new Login();
        }
    }
}

.............DB Connection.....................

package textiles;

import java.sql.Connection;
import java.sql.DriverManager;

public class DBConnection {

    public static Connection getConnection() {

        Connection con = null;

        try {

//            Class.forName("/");

            con = DriverManager.getConnection(
                    "jdbc:mysql://localhost:3306/textiles_db",
                    "root",
                    "root"
            );

            System.out.println("Connected to MySQL!");

        } catch (Exception e) {
            e.printStackTrace();
        }

        return con;
    }
}

....................Login..................
package textiles;

 
import java.awt.Color;
import java.awt.Font;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JOptionPane;
import javax.swing.JPasswordField;
import javax.swing.JTextField;

public class Login implements ActionListener {

    JFrame frame;

    JTextField usernameText;
    JPasswordField passwordText;

    JButton loginButton;
    JButton registerButton;

    public Login() {

        frame = new JFrame("Global Banking Management System");

        frame.setSize(800, 800);
        frame.setLayout(null);
        frame.setLocationRelativeTo(null);

        frame.getContentPane().setBackground(
                new Color(245, 235, 220)
        );

        // TITLE

        JLabel title = new JLabel("Global Banking Saving Account");

        title.setBounds(175, 50, 300, 50);

        title.setFont(
                new Font("Serif", Font.BOLD, 32)
        );

        title.setForeground(
                new Color(100, 30, 40)
        );

        frame.add(title);

        JLabel welcome = new JLabel("Welcome Back!");

        welcome.setBounds(220, 105, 200, 30);

        welcome.setFont(
                new Font("Arial", Font.BOLD, 18)
        );

        frame.add(welcome);

        // USERNAME

        JLabel usernameLabel = new JLabel("Username");

        usernameLabel.setBounds(100, 180, 150, 30);

        usernameLabel.setFont(
                new Font("Arial", Font.BOLD, 16)
        );

        frame.add(usernameLabel);

        usernameText = new JTextField();

        usernameText.setBounds(
                250, 180, 250, 35
        );

        frame.add(usernameText);

        // PASSWORD

        JLabel passwordLabel = new JLabel("Password");

        passwordLabel.setBounds(100, 240, 150, 30);

        passwordLabel.setFont(
                new Font("Arial", Font.BOLD, 16)
        );

        frame.add(passwordLabel);

        passwordText = new JPasswordField();

        passwordText.setBounds(
                250, 240, 250, 35
        );

        frame.add(passwordText);

        // LOGIN BUTTON

        loginButton = new JButton("LOGIN");

        loginButton.setBounds(
                200, 320, 200, 45
        );

        loginButton.setFont(
                new Font("Arial", Font.BOLD, 16)
        );

        loginButton.setBackground(
                new Color(100, 30, 40)
        );

        loginButton.setForeground(Color.yellow);

        loginButton.setFocusPainted(false);

        loginButton.addActionListener(this);

        frame.add(loginButton);

        // REGISTER BUTTON

        registerButton =
                new JButton("New User? REGISTER");

        registerButton.setBounds(
                180, 400, 240, 35
        );

        registerButton.setBorderPainted(false);

        registerButton.setContentAreaFilled(false);

        registerButton.setFont(
                new Font("Arial", Font.PLAIN, 14)
        );

        registerButton.addActionListener(this);

        frame.add(registerButton);

        frame.setDefaultCloseOperation(
                JFrame.EXIT_ON_CLOSE
        );

        frame.setVisible(true);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

        if (e.getSource() == loginButton) {

            String username =
                    usernameText.getText().trim();

            String password =
                    new String(
                            passwordText.getPassword()
                    ).trim();

            if (username.isEmpty() ||
                password.isEmpty()) {

                JOptionPane.showMessageDialog(
                        frame,
                        "Please enter username and password!"
                );

                return;
            }

            String sql =
                    "SELECT name FROM users "
                    + "WHERE username = ? AND password = ?";

            try {

                Connection con =
                        DBConnection.getConnection();

                PreparedStatement ps =
                        con.prepareStatement(sql);

                ps.setString(1, username);
                ps.setString(2, password);

                ResultSet rs =
                        ps.executeQuery();

                if (rs.next()) {

                    String name =
                            rs.getString("name");

                    JOptionPane.showMessageDialog(
                            frame,
                            "Login Successful!"
                    );

                    frame.dispose();

                    new Dashboard(name);

                } else {

                    JOptionPane.showMessageDialog(
                            frame,
                            "Invalid Username or Password!"
                    );
                }

                rs.close();
                ps.close();
                con.close();

            } catch (Exception ex) {

                JOptionPane.showMessageDialog(
                        frame,
                        "Database Error!\n"
                        + ex.getMessage()
                );

                ex.printStackTrace();
            }
        }

        // REGISTER

        if (e.getSource() == registerButton) {

            frame.dispose();

            new Register();
        }
    }

    public static void main(String[] args) {

        new Login();
    }
}
.....................Register..........................
package textiles;

 
import java.awt.Color;
import java.awt.Font;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.sql.Connection;
import java.sql.PreparedStatement;

import javax.swing.ImageIcon;
import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JOptionPane;
import javax.swing.JPasswordField;
import javax.swing.JTextField;

public class Register implements ActionListener {

    JFrame frame;

    JLabel title;
    JLabel nameLabel;
    JLabel usernameLabel;
    JLabel passwordLabel;
    JLabel phoneLabel;

    JTextField nameText;
    JTextField usernameText;
    JTextField phoneText;

    JPasswordField passwordText;

    JButton registerButton;
    JButton loginButton;

    public Register() {

        frame = new JFrame("Banking Management System");

        frame.setSize(600, 650);
        ImageIcon icon=new ImageIcon("C:\\Users\\Administrator\\Downloads\\taylor-r-clkI9_xcuis-unsplash.jpg");
        JLabel bglabel=new JLabel(icon);
        frame.setContentPane(bglabel);
        bglabel.setBounds(0, 0, 800, 500);

         frame.setLayout(null);
        frame.setLocationRelativeTo(null);
        frame.getContentPane().setBackground(new Color(245, 250, 220));

        // TITLE

        title = new JLabel("Banking System");
        title.setBounds(170, 40, 300, 50);
        title.setFont(new Font("Serif", Font.BOLD, 30));
        title.setForeground(new Color(100, 30, 40));

        frame.add(title);

        JLabel subTitle = new JLabel("Create Your Account");
        subTitle.setBounds(190, 90, 250, 30);
        subTitle.setFont(new Font("Arial", Font.BOLD, 18));

        frame.add(subTitle);

        // NAME

        nameLabel = new JLabel("Name");
        nameLabel.setBounds(100, 150, 150, 30);
        nameLabel.setFont(new Font("Arial", Font.BOLD, 16));

        frame.add(nameLabel);

        nameText = new JTextField();
        nameText.setBounds(250, 150, 250, 35);

        frame.add(nameText);

        // USERNAME

        usernameLabel = new JLabel("Username");
        usernameLabel.setBounds(100, 210, 150, 30);
        usernameLabel.setFont(new Font("Arial", Font.BOLD, 16));

        frame.add(usernameLabel);

        usernameText = new JTextField();
        usernameText.setBounds(250, 210, 250, 35);

        frame.add(usernameText);

        // PASSWORD

        passwordLabel = new JLabel("Password");
        passwordLabel.setBounds(100, 270, 150, 30);
        passwordLabel.setFont(new Font("Arial", Font.BOLD, 16));

        frame.add(passwordLabel);

        passwordText = new JPasswordField();
        passwordText.setBounds(250, 270, 250, 35);

        frame.add(passwordText);

        // PHONE

        phoneLabel = new JLabel("Phone");
        phoneLabel.setBounds(100, 330, 150, 30);
        phoneLabel.setFont(new Font("Arial", Font.BOLD, 16));

        frame.add(phoneLabel);

        phoneText = new JTextField();
        phoneText.setBounds(250, 330, 250, 35);

        frame.add(phoneText);

        // REGISTER BUTTON

        registerButton = new JButton("REGISTER");

        registerButton.setBounds(200, 400, 200, 45);

        registerButton.setFont(
                new Font("Arial", Font.BOLD, 16)
        );

        registerButton.setBackground(
                new Color(100, 30, 40)
        );

        registerButton.setForeground(Color.GREEN);

        registerButton.setFocusPainted(false);

        registerButton.addActionListener(this);

        frame.add(registerButton);

        // LOGIN BUTTON

        loginButton = new JButton("Already have an account? LOGIN");

        loginButton.setBounds(160, 470, 280, 35);

        loginButton.setFont(
                new Font("Arial", Font.PLAIN, 14)
        );

        loginButton.setBorderPainted(false);
        loginButton.setContentAreaFilled(false);

        loginButton.addActionListener(this);

        frame.add(loginButton);

        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        frame.setVisible(true);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

        if (e.getSource() == registerButton) {

            String name = nameText.getText().trim();
            String username = usernameText.getText().trim();

            String password =
                    new String(passwordText.getPassword()).trim();

            String phone = phoneText.getText().trim();

            // VALIDATION

            if (name.isEmpty() ||
                username.isEmpty() ||
                password.isEmpty() ||
                phone.isEmpty()) {

                JOptionPane.showMessageDialog(
                        frame,
                        "Please fill all fields!"
                );

                return;
            }

            // DATABASE INSERT

            String sql =
                    "INSERT INTO users(name, username, password, phone) "
                    + "VALUES (?, ?, ?, ?)";

            try {

                Connection con =
                        DBConnection.getConnection();

                PreparedStatement ps =
                        con.prepareStatement(sql);

                ps.setString(1, name);
                ps.setString(2, username);
                ps.setString(3, password);
                ps.setString(4, phone);

                int result = ps.executeUpdate();

                if (result > 0) {

                    JOptionPane.showMessageDialog(
                            frame,
                            "Registration Successful!"
                    );

                    frame.dispose();

                    new Login();
                }

                ps.close();
                con.close();

            } catch (Exception ex) {

                JOptionPane.showMessageDialog(
                        frame,
                        "Registration Failed!\n"
                        + ex.getMessage()
                );

                ex.printStackTrace();
            }
        }

        // LOGIN PAGE

        if (e.getSource() == loginButton) {

            frame.dispose();

            new Login();
        }
    }

    public static void main(String[] args) {

        new Register();
    }
}
......................................................
